services:
  - type: web
    name: foncier-agricole
    runtime: python
    buildCommand: pip install -r backend/requirements.txt && pip install fpdf2 pyproj
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
